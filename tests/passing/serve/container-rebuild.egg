(check (= v1 v2))

(check (MyVec v2))

(check (= (MyVec v1) (MyVec v2)))

(check (= (MyVec v3) (MyVec (vec-of (Num 6) (Num 7)))))

(extract (MyVec v1))

(check (= (MyVec v1) (MyVec v2)))

(extract myvec 0)
