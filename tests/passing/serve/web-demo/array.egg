(check (= test1 (Num 42)))

(check (neq r1 r2))

(check (neq r1 (add r1 (Num 17))))

(check (= test2 (select mem1 (add r1 (Num 17)))))

(check (= test3 (select mem1 (add r1 r3))))

(check (= test4 r1))
